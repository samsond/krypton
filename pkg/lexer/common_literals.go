package lexer

// contains the common literals used in the common DSL
const (
	separatorValue   = "---"
	rightbraceValue  = "}"
	leftbraceValue   = "{"
	portsPrefix      = "ports {"
	portsTokenValue  = "ports"
	portPrefix       = "port:"
	targetPortPrefix = "targetPort:"
)
