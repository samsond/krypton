apiVersion: v1
kind: Service
metadata:
  name: {{ .Name }}
  namespace: {{ .Namespace }}
  labels:
    app: {{ .Name }}
spec:
  selector:
    app: {{ .Name }}
  ports:
  {{- range $port, $targetPort := .Ports }}
  - port: {{ $port }}
    targetPort: {{ $targetPort }}
  {{- end }}